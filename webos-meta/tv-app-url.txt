https://ytube-umber.vercel.app/

# ↑ DÒNG ĐẦU TIÊN Ở TRÊN là link Vercel thật của app — sửa đúng 1 dòng đó thôi.
#
# Cách lấy link: vào https://vercel.com > mở project Ytube của bạn > copy link dạng
# "https://ytube-umber.vercel.app/" ở đầu trang (link mà bạn vẫn mở trên điện thoại để xem app).
#
# Lưu ý:
# - Phải bắt đầu bằng https:// (không phải http://).
# - KHÔNG có dấu "/" ở cuối.
# - Mọi dòng bắt đầu bằng dấu # (như các dòng này) chỉ là ghi chú, script sẽ bỏ qua.
#
# Vì sao cần file này: app trên TV giờ chạy theo kiểu "hosted" — file .ipk cài lên TV chỉ
# là 1 trang mỏng, nhiệm vụ duy nhất là mở đúng link ở trên. Nhờ vậy app chạy qua https://
# thật (không còn file://), nên YouTube mới chịu phát video (hết lỗi 153). Đổi lại, mỗi lần
# cập nhật code bạn CHỈ cần deploy lại Vercel — không phải build/đóng gói/cài lại .ipk nữa.
